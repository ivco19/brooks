# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = style

[flake8]
exclude = migrations, .tox, settings.py, tests.py
ignore = E402

[pytest]
addopts = -n "auto"


[testenv:style]
skip_install = True
usedevelop = False
deps = flake8
commands =
    flake8 site/ {posargs}
